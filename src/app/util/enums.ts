export enum restStatus {
  OPEN,
  CLOSED
}

export enum Weekday {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday
}

export enum Device {
  web,
  MOBILE,
  TABLE,
  ANDROID,
  IPHONE,
  IPHONE5,
  IPHONE6,
  DESKTOP,
  NATIVE_MOBILE,
  NATIVE_DESKTOP,
  NATIVE_IOS,
  UNIVERSAL
}

export enum eLang {
  consoleErr
}